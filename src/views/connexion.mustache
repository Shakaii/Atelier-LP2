{{> header}}
        <h1>{{title}}</h1>
        <div class="connexionPannel">
            <label >Adresse email <input name="mail" type="text"></label>
            <label >Mot de passe <input name="pass" type="password"></label>
            <input type = "submit" value="Connexion">
        </div>
{{> footer}}
