{{> header}}
    <h1 class="titreW">Profil de l'utilisateur. Vous pouvez consulter le status de vos coffrets et modifier vos informations personnelles.</h1>
    <div class="container">
        {{#user.boxes}}
        {{^isCurrent}}
            <div class="card nonCurrent">
        {{/isCurrent}}
        {{#isCurrent}}
            <div class="card current">
        {{/isCurrent}}
            <h1 class="card-title">
                {{#isCurrent}}
                    Coffret en cours
                {{/isCurrent}}
                {{^isCurrent}}
                    Autre coffret
                {{/isCurrent}}
            </h1>
            <div class="description-box">
                Nombre de prestation(s) : {{prestations.length}}
                {{#box.isPaid}}
                    Le coffret est validé et a été transmis au destinataire !
                    {{#box.isOpened}}
                </br>Il a été ouvert !
                    {{/box.isOpened}}
                    {{^box.isOpened}}
                </br>Il n'a pas encore été ouvert.
                    {{/box.isOpened}}
                {{/box.isPaid}}
                {{^box.isPaid}}
                    </br>Le coffret n'est pas validé !
                {{/box.isPaid}}
                </div>
            {{#urlGift}}
            <input data-clipboard-text="{{host}}/box/gift/{{user.id}}/{{urlGift}}" disabled="disabled" value={{host}}/box/gift/{{user.id}}/{{urlGift}}> 
            <div class="urlGift" data-clipboard-text="{{host}}/box/gift/{{user.id}}/{{urlGift}}">Copier dans le presse papier</div>
            {{/urlGift}}
            {{^urlGift}}
                {{#box.isPaid}}
                    <a class="link" href=/box/giftGenerate/{{_id}}>Génerer votre Url cadeau !</a> 
                {{/box.isPaid}}
            <a class="link" href="/changeCurr/{{_id}}">Définir en temps que coffret courant</a> 
            {{/urlGift}}
            {{#box.isPaid}}{{/box.isPaid}}{{^box.isPaid}}
            
            {{/box.isPaid}}
            {{#box.isPaid}}{{/box.isPaid}}{{^box.isPaid}}
                {{#box.isPot}}
                    <input data-clipboard-text="{{host}}/box/gift/{{user.id}}/{{urlPot}}" disabled="disabled" value={{host}}/box/gift/{{user.id}}/{{urlGift}}> 
                    <div class="urlGift" data-clipboard-text="{{host}}/box/gift/{{user.id}}/{{urlGift}}">Copier dans le presse papier</div>
                {{/box.isPot}}
                {{^box.isPot}}
                    <a class="link" href=/box/giftGenerate/{{_id}}>Passer en mode cagnotte</a>
                    <a class="link" href="test">Payer</a>
                {{/box.isPot}}
            {{/box.isPaid}}
            <div class="card-footer">
                <a class="zoom" href=/box/{{_id}}><i class="fas fa-search-plus"></i></a>
                {{#box.isPaid}}{{/box.isPaid}}{{^box.isPaid}}
                    <a class="rmPrest" href=/delete/{{_id}}><i class="fas fa-trash-alt"></i></a>
                {{/box.isPaid}}
            </div>
        </div>
        {{/user.boxes}}
    </div>
    <a href="/profile/modify" class="modifProfil">Modifier mon mot de passe</a>
{{> footer}} 


<script src="https://cdnjs.cloudflare.com/ajax/libs/clipboard.js/2.0.0/clipboard.min.js"></script>
<script src="https://unpkg.com/tippy.js@3/dist/tippy.all.min.js"></script>
<script>
    let clipboard = new ClipboardJS('.urlGift');
    tippy('.urlGift',{
		content: "copié!",
		trigger: "click",
		arrow: true,
		animation: "fade",
		onShow: function(){
			setTimeout(function(){tippy.hideAllPoppers()},800)
			
		}
	});
</script> 
