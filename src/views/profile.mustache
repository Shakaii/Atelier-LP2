{{> header}}
    <h1 class="titreProfil">Profil de l'utilisateur {{user.email}}</h1>
    <div class="container">
        {{#user.boxes}}
        {{^isCurrent}}
            <div class="box nonCurrent">
        {{/isCurrent}}
        {{#isCurrent}}
            <div class="box current">
        {{/isCurrent}}
            <p class="card-title">
                {{#isCurrent}}
                    Coffret en cours
                {{/isCurrent}}
                {{^isCurrent}}
                    Autre coffret
                {{/isCurrent}}
            </p>
                Nombre de prestations : {{prestations.length}}
                {{#box.isPaid}}
                    Votre coffret est validé et transmis au destinataire !
                    {{#box.isOpened}}
                        Il a été ouvert !
                    {{/box.isOpened}}
                    {{^box.isOpened}}
                        Il n'a pas encore été ouvert.
                    {{/box.isOpened}}
                {{/box.isPaid}}
                {{^box.isPaid}}
                    </br>Votre coffret n'est pas validé !
                    <a href=/box/{{_id}}>Modifier le contenu du coffret</a>
                {{/box.isPaid}}
            </br>
            {{#urlGift}}
            <input disabled="disabled" value={{host}}/box/gift/{{user.id}}/{{urlGift}}> 
            {{/urlGift}}
            {{^urlGift}}
            <a href=/box/giftGenerate/{{_id}}>Génerer votre Url cadeau !</a> 
            {{/urlGift}}
        </div>
        {{/user.boxes}}
    </div>
    <a href="/profile/modify" class="modifProfil">Modifier ses informations personnelles</a>
{{> footer}}